# Микросервисная архитектура: Users и Orders

## 📋 Описание проекта

Проект реализует два микросервиса с JWT авторизацией:
- **Users Service** - регистрация и авторизация пользователей
- **Orders Service** - управление заказами с защитой от несанкционированного доступа
- **API Gateway** - маршрутизация запросов к микросервисам

## 🏗️ Архитектура проекта

```
project/
├── users-service/          (Порт 3001)
│   ├── app.js
│   ├── app.test.js
│   ├── package.json
│   ├── jest.config.js
│   └── node_modules/
├── orders-service/         (Порт 3002)
│   ├── app.js
│   ├── authMiddleware.js
│   ├── app.test.js
│   ├── package.json
│   ├── jest.config.js
│   └── node_modules/
├── api-gateway/            (Порт 3000)
│   ├── app.js
│   ├── package.json
│   └── node_modules/
├── docker-compose.yml
├── .env
└── Postman_Collection.json
```

## 🚀 Быстрый старт

### 1. Установка зависимостей

Для каждого сервиса:

```bash
# Users Service
cd project/users-service
npm install

# Orders Service
cd project/orders-service
npm install

# API Gateway
cd project/api-gateway
npm install
```

### 2. Настройка переменных окружения

Файл `.env` в корне проекта уже создан:
```
JWT_SECRET=super-secret-for-dev
PORT=3001
```

### 3. Запуск сервисов

**Терминал 1 - Users Service:**
```bash
cd project/users-service
npm start
# Слушает на http://localhost:3001
```

**Терминал 2 - Orders Service:**
```bash
cd project/orders-service
npm start
# Слушает на http://localhost:3002
```

**Терминал 3 - API Gateway:**
```bash
cd project/api-gateway
npm start
# Слушает на http://localhost:3000
```

## ✅ Тестирование

### Jest тесты

**Users Service:**
```bash
cd project/users-service
npm test
```

**Orders Service:**
```bash
cd project/orders-service
npm test
```

### Postman тестирование

1. Откройте Postman
2. Импортируйте `Postman_Collection.json`
3. Следуйте последовательности запросов:

#### 🔐 Этап 1: Регистрация и логин

**1. Register User 1**
```
POST http://localhost:3001/v1/users/register
Body: {
  "email": "user1@example.com",
  "password": "password123"
}
Ожидаемый результат: 201 Created
```

**2. Login User 1**
```
POST http://localhost:3001/v1/users/login
Body: {
  "email": "user1@example.com",
  "password": "password123"
}
Ожидаемый результат: 200 OK
Ответ: {
  "message": "Успешная авторизация",
  "token": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...",
  "user": {
    "userId": 1,
    "email": "user1@example.com"
  }
}
```

#### 📦 Этап 2: Создание заказа

**3. Create Order (User 1)**
```
POST http://localhost:3002/v1/orders
Headers: Authorization: Bearer <token_from_step_2>
Body: {
  "items": [
    {
      "productId": 1,
      "productName": "Laptop",
      "quantity": 1,
      "price": 999
    }
  ],
  "totalAmount": 999
}
Ожидаемый результат: 201 Created
Ответ: {
  "message": "Заказ успешно создан",
  "order": {
    "orderId": 1,
    "userId": 1,
    "items": [...],
    "totalAmount": 999,
    "status": "pending",
    "createdAt": "...",
    "updatedAt": "..."
  }
}
```

#### 👁️ Этап 3: Просмотр заказов

**4. Get My Orders (User 1)**
```
GET http://localhost:3002/v1/orders
Headers: Authorization: Bearer <token_from_step_2>
Ожидаемый результат: 200 OK
Ответ: {
  "message": "Заказы пользователя",
  "count": 1,
  "orders": [
    {
      "orderId": 1,
      "userId": 1,
      "status": "pending",
      ...
    }
  ]
}
```

#### 🔒 Этап 4: Защита от несанкционированного доступа

**5. Register User 2 и Login User 2**
```
POST http://localhost:3001/v1/users/register
Body: {
  "email": "user2@example.com",
  "password": "password456"
}

POST http://localhost:3001/v1/users/login
Body: {
  "email": "user2@example.com",
  "password": "password456"
}
```

**6. Try Update Other User Order (403 Forbidden)**
```
PATCH http://localhost:3002/v1/orders/1
Headers: Authorization: Bearer <token_user2>
Body: {
  "totalAmount": 500
}
Ожидаемый результат: 403 Forbidden
Ответ: {
  "error": "У вас нет доступа к этому ресурсу"
}
```

#### ❌ Этап 5: Отмена заказа

**7. Cancel Order (User 1)**
```
PATCH http://localhost:3002/v1/orders/1/cancel
Headers: Authorization: Bearer <token_user1>
Ожидаемый результат: 200 OK
Ответ: {
  "message": "Заказ успешно отменен",
  "order": {
    "orderId": 1,
    "userId": 1,
    "status": "cancelled",
    "cancelledAt": "2025-12-06T...",
    "updatedAt": "2025-12-06T..."
  }
}
```

**8. Get Orders After Cancel (Проверка статуса)**
```
GET http://localhost:3002/v1/orders
Headers: Authorization: Bearer <token_user1>
Ожидаемый результат: 200 OK
Ответ должен содержать заказ со статусом "cancelled"
```

## 📚 API Endpoints

### Users Service (Порт 3001)

| Метод | Endpoint | Описание | Авторизация |
|-------|----------|---------|-------------|
| POST | `/v1/users/register` | Регистрация | ❌ Нет |
| POST | `/v1/users/login` | Авторизация | ❌ Нет |
| GET | `/v1/users/me` | Текущий пользователь | ✅ JWT |

### Orders Service (Порт 3002)

| Метод | Endpoint | Описание | Авторизация | Проверка владельца |
|-------|----------|---------|-------------|-------------------|
| POST | `/v1/orders` | Создать заказ | ✅ JWT | N/A |
| GET | `/v1/orders` | Получить свои заказы | ✅ JWT | N/A |
| PATCH | `/v1/orders/:id` | Изменить заказ | ✅ JWT | ✅ ownerId |
| PATCH | `/v1/orders/:id/cancel` | Отменить заказ | ✅ JWT | ✅ ownerId |

### API Gateway (Порт 3000)

| Endpoint | Перенаправление |
|----------|-----------------|
| `/v1/users/*` | http://localhost:3001 |
| `/v1/orders/*` | http://localhost:3002 |
| `/health` | Проверка статуса |

## 🔐 Механизм авторизации

### JWT Токен

Структура payload:
```json
{
  "userId": 1,
  "email": "user@example.com",
  "iat": 1670000000,
  "exp": 1670086400
}
```

**Время жизни токена:** 24 часа

### Передача токена

```
Headers:
Authorization: Bearer eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
```

### Ошибки авторизации

| Код | Сообщение | Причина |
|-----|-----------|--------|
| 401 | Токен не найден | Отсутствует заголовок Authorization |
| 403 | Неверный токен | Токен подделан или истек |
| 403 | Токен истек | Время жизни токена истекло |
| 403 | У вас нет доступа | Попытка доступа к чужому ресурсу (ownerId) |

## 🧪 Примеры тест-кейсов

### ✓ Успешные сценарии

1. ✅ Регистрация нового пользователя
2. ✅ Авторизация с верными учетными данными
3. ✅ Создание заказа с действительным токеном
4. ✅ Просмотр своих заказов
5. ✅ Изменение своего заказа
6. ✅ Отмена своего заказа

### ✗ Ошибочные сценарии

1. ❌ Попытка регистрации с существующим email
2. ❌ Авторизация с неверным паролем
3. ❌ Запрос без токена → 401
4. ❌ Запрос с поддельным токеном → 403
5. ❌ User 2 пытается изменить заказ User 1 → 403
6. ❌ Попытка отменить уже отмененный заказ → 400

## 📝 Примеры ответов

### Успешная регистрация
```json
HTTP 201 Created
{
  "message": "Пользователь успешно зарегистрирован",
  "userId": 1,
  "email": "user1@example.com"
}
```

### Успешная авторизация
```json
HTTP 200 OK
{
  "message": "Успешная авторизация",
  "token": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...",
  "user": {
    "userId": 1,
    "email": "user1@example.com"
  }
}
```

### Ошибка: 403 Forbidden
```json
HTTP 403 Forbidden
{
  "error": "У вас нет доступа к этому ресурсу"
}
```

### Ошибка: 400 Bad Request
```json
HTTP 400 Bad Request
{
  "error": "Заказ уже отменен"
}
```

## 🐳 Docker (опционально)

```bash
# Запуск всех сервисов в контейнерах
docker-compose up

# Остановка
docker-compose down
```

## 📦 Зависимости

- **express** ^4.18.2 - Web framework
- **jsonwebtoken** ^9.0.2 - JWT генерация и проверка
- **bcryptjs** ^2.4.3 - Хеширование паролей
- **dotenv** ^16.3.1 - Переменные окружения
- **http-proxy-middleware** ^2.0.6 - Проксирование (API Gateway)
- **jest** ^29.0.0 - Тестирование (dev)
- **supertest** ^6.3.0 - HTTP тестирование (dev)

## 🔍 Troubleshooting

### Ошибка: Port already in use

Если порт занят, измените PORT в `.env` или убейте процесс:
```bash
# Windows
netstat -ano | findstr :3001
taskkill /PID <PID> /F

# Linux/Mac
lsof -i :3001
kill -9 <PID>
```

### Ошибка: Cannot find module

Убедитесь, что установлены все зависимости:
```bash
npm install
```

### Тесты не запускаются

Проверьте версию Node.js:
```bash
node --version
# Требуется >= 14.0.0
```

## 📞 Контакты и поддержка

При возникновении проблем проверьте:
1. Статус всех трех сервисов в терминалах
2. Корректность JWT токена в Postman
3. Логи в консоли каждого сервиса

---

**Версия:** 1.0.0  
**Дата:** 6 декабря 2025  
**Статус:** Production Ready ✅
