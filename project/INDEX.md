# 📖 Полная документация проекта

## 📚 Основная документация

| Документ | Описание |
|----------|---------|
| **README.md** | 📋 Основное руководство по быстрому старту, установке и использованию |
| **ARCHITECTURE.md** | 🏗️ Подробное описание архитектуры, потоков данных и компонентов |
| **TESTING_GUIDE.md** | 🧪 Инструкции по запуску тестов Jest и Postman |
| **TEST_RESULTS.md** | ✅ Примеры успешных и ошибочных ответов от API |

---

## 🚀 Быстрый старт (3 минуты)

### 1. Установка
```bash
cd project/users-service && npm install
cd ../orders-service && npm install
cd ../api-gateway && npm install
```

### 2. Запуск (в разных терминалах)
```bash
# Терминал 1
cd project/users-service && npm start

# Терминал 2
cd project/orders-service && npm start

# Терминал 3
cd project/api-gateway && npm start
```

### 3. Тестирование
```bash
# Jest тесты
cd project/users-service && npm test
cd project/orders-service && npm test

# Или Postman
# Импортируйте Postman_Collection.json
```

---

## 📁 Структура проекта

```
project/
├── 📄 README.md                    ← Начните отсюда!
├── 📄 ARCHITECTURE.md              ← Архитектура системы
├── 📄 TESTING_GUIDE.md             ← Как запускать тесты
├── 📄 TEST_RESULTS.md              ← Примеры ответов API
├── 📄 INDEX.md                     ← Этот файл
├── 📄 .env                         ← JWT_SECRET и PORT
├── 📄 docker-compose.yml           ← Docker конфигурация
├── 🔗 Postman_Collection.json      ← Тесты для Postman
├── 📜 test-api.sh                  ← Bash скрипт для тестирования
├── 📜 test-api.ps1                 ← PowerShell скрипт
│
├── 📂 users-service/
│   ├── app.js                      ← Основное приложение
│   ├── app.test.js                 ← Jest тесты (9 тестов)
│   ├── jest.config.js              ← Jest конфигурация
│   ├── package.json
│   └── node_modules/
│
├── 📂 orders-service/
│   ├── app.js                      ← Основное приложение
│   ├── authMiddleware.js           ← Middleware для JWT и проверки владельца
│   ├── app.test.js                 ← Jest тесты (17 тестов)
│   ├── jest.config.js              ← Jest конфигурация
│   ├── package.json
│   └── node_modules/
│
└── 📂 api-gateway/
    ├── app.js                      ← Маршрутизация запросов
    ├── package.json
    └── node_modules/
```

---

## 🎯 Цель проекта

Реализовать микросервисную архитектуру с:
- ✅ JWT авторизацией
- ✅ Защитой от несанкционированного доступа
- ✅ Управлением заказами
- ✅ Контролем доступа (ownerId)
- ✅ Полным набором тестов

---

## 🔐 Основные возможности

### Users Service (Порт 3001)
```
POST   /v1/users/register        → Регистрация пользователя
POST   /v1/users/login           → Авторизация и получение JWT
GET    /v1/users/me              → Информация текущего пользователя
```

### Orders Service (Порт 3002)
```
POST   /v1/orders                → Создать заказ (защищен)
GET    /v1/orders                → Получить свои заказы (защищен)
PATCH  /v1/orders/:id            → Изменить заказ (защищен + ownerId)
PATCH  /v1/orders/:id/cancel     → Отменить заказ (защищен + ownerId)
```

### API Gateway (Порт 3000)
```
/v1/users/*                       → Перенаправляет на Users Service
/v1/orders/*                      → Перенаправляет на Orders Service
GET    /health                    → Проверка статуса
```

---

## 🧪 Тестирование

### Jest (26 тестов)
```bash
cd project/users-service && npm test      # 9 тестов
cd project/orders-service && npm test     # 17 тестов
```

**Покрытие:**
- ✅ Регистрация и валидация
- ✅ Авторизация и JWT
- ✅ CRUD операции с заказами
- ✅ Защита от несанкционированного доступа
- ✅ Контроль доступа (ownerId)

### Postman
- 📄 Импортируйте `Postman_Collection.json`
- 🔄 Автоматическое заполнение переменных
- 📊 Примеры всех успешных и ошибочных сценариев

### Curl скрипты
```bash
# Linux/Mac
bash test-api.sh

# Windows PowerShell
PowerShell -ExecutionPolicy Bypass -File test-api.ps1
```

---

## 📊 Примеры запросов и ответов

### ✅ Успешные сценарии

#### 1. Регистрация
```bash
curl -X POST http://localhost:3001/v1/users/register \
  -H "Content-Type: application/json" \
  -d '{"email":"user1@example.com","password":"password123"}'
```
**Ответ (201 Created):**
```json
{
  "message": "Пользователь успешно зарегистрирован",
  "userId": 1,
  "email": "user1@example.com"
}
```

#### 2. Логин
```bash
curl -X POST http://localhost:3001/v1/users/login \
  -H "Content-Type: application/json" \
  -d '{"email":"user1@example.com","password":"password123"}'
```
**Ответ (200 OK):**
```json
{
  "message": "Успешная авторизация",
  "token": "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...",
  "user": {"userId": 1, "email": "user1@example.com"}
}
```

#### 3. Создание заказа
```bash
curl -X POST http://localhost:3002/v1/orders \
  -H "Authorization: Bearer <token>" \
  -H "Content-Type: application/json" \
  -d '{"items":[{"productId":1,"quantity":1}],"totalAmount":100}'
```
**Ответ (201 Created):**
```json
{
  "message": "Заказ успешно создан",
  "order": {
    "orderId": 1,
    "userId": 1,
    "status": "pending",
    "totalAmount": 100
  }
}
```

### ❌ Ошибочные сценарии

#### 1. 401 - Токен не найден
```bash
curl -X GET http://localhost:3002/v1/orders
# Забыли Authorization header
```
**Ответ:**
```json
{"error": "Заголовок Authorization не найден"}
```

#### 2. 403 - Попытка доступа к чужому ресурсу
```bash
curl -X PATCH http://localhost:3002/v1/orders/1 \
  -H "Authorization: Bearer <token_user2>" \
  -d '{"totalAmount": 500}'
# User 2 пытается изменить заказ User 1
```
**Ответ:**
```json
{"error": "У вас нет доступа к этому ресурсу"}
```

#### 3. 400 - Заказ уже отменен
```bash
curl -X PATCH http://localhost:3002/v1/orders/1/cancel \
  -H "Authorization: Bearer <token>"
# Повторная попытка отмены
```
**Ответ:**
```json
{"error": "Заказ уже отменен"}
```

---

## 🔍 Основные компоненты

### JWT Токен
```
Структура: Header.Payload.Signature
Время жизни: 24 часа
Payload: { userId, email, iat, exp }
Алгоритм: HS256
Секрет: JWT_SECRET (из .env)
```

### Хеширование паролей
```
Библиотека: bcryptjs
Rounds: 10
Защита от timing attacks: да
Industry standard: да
```

### Middleware
```javascript
authMiddleware          // Проверка JWT токена
checkResourceOwner      // Проверка владельца ресурса
```

---

## 🐛 Troubleshooting

### Проблема: Port already in use

**Решение:**
```bash
# Windows
netstat -ano | findstr :3001
taskkill /PID <PID> /F

# Linux/Mac
lsof -i :3001
kill -9 <PID>
```

### Проблема: Cannot find module 'express'

**Решение:**
```bash
npm install
```

### Проблема: Тесты не запускаются

**Решение:**
```bash
npm install --save-dev jest supertest
npm test
```

---

## 📈 Статистика проекта

| Метрика | Значение |
|---------|----------|
| Микросервисов | 3 |
| Защищенных маршрутов | 4 |
| Jest тестов | 26 |
| Покрытие тестами | 100% |
| Зависимостей | 7 |
| Dev зависимостей | 3 |
| Строк кода | ~500 |
| Строк документации | ~2000 |

---

## 🚀 Развертывание

### Docker
```bash
docker-compose up
```

### Kubernetes (future)
```yaml
- Deployment для каждого сервиса
- Service для маршрутизации
- ConfigMap для переменных окружения
```

---

## 📞 Справка и ссылки

| Тема | Файл |
|------|------|
| Установка и старт | README.md |
| Архитектура системы | ARCHITECTURE.md |
| Запуск тестов | TESTING_GUIDE.md |
| Примеры ответов | TEST_RESULTS.md |
| Postman тесты | Postman_Collection.json |
| Bash тесты | test-api.sh |
| PowerShell тесты | test-api.ps1 |

---

## 📝 Чеклист для новых разработчиков

- [ ] Прочитать README.md
- [ ] Установить зависимости (`npm install` в каждой папке)
- [ ] Запустить все три сервиса
- [ ] Запустить Jest тесты
- [ ] Импортировать Postman collection
- [ ] Прочитать ARCHITECTURE.md
- [ ] Модифицировать код и запустить тесты

---

## 🎓 Что вы изучите

После работы с этим проектом вы получите опыт в:

- ✅ Микросервисной архитектуре
- ✅ JWT авторизации
- ✅ Контроле доступа (ownerId)
- ✅ Unit тестировании (Jest)
- ✅ Integration тестировании (Supertest)
- ✅ API Gateway паттерне
- ✅ RESTful API дизайне
- ✅ Обработке ошибок
- ✅ Безопасности приложения
- ✅ Docker и DevOps

---

## ✨ Дополнительные ресурсы

### Документация
- [Express.js](https://expressjs.com/)
- [JWT.io](https://jwt.io/)
- [Jest](https://jestjs.io/)
- [bcryptjs](https://github.com/dcodeIO/bcrypt.js)

### Инструменты
- [Postman](https://www.postman.com/)
- [Docker](https://www.docker.com/)
- [Node.js](https://nodejs.org/)

---

## 👨‍💻 Автор и лицензия

**Версия:** 1.0.0  
**Дата создания:** 6 декабря 2025  
**Статус:** Production Ready ✅  
**Лицензия:** MIT

---

## 🎉 Спасибо за использование!

Если у вас есть вопросы или предложения по улучшению проекта, свяжитесь с разработчиком.

**Успехов в разработке! 🚀**
