# 🧪 Инструкция по запуску тестов

## Быстрый старт

### 1️⃣ Users Service - Jest тесты

```bash
cd project/users-service

# Установка зависимостей (если еще не установлены)
npm install

# Запуск тестов
npm test

# Запуск тестов с покрытием
npm test -- --coverage

# Запуск в watch режиме
npm test -- --watch
```

**Ожидаемый результат:**
```
PASS  app.test.js (2.456 s)
  Users Service Tests
    POST /v1/users/register
      ✓ должен успешно зарегистрировать нового пользователя
      ✓ должен вернуть ошибку 400, если email уже существует
      ✓ должен вернуть ошибку 400, если отсутствует email
      ✓ должен вернуть ошибку 400, если отсутствует пароль
    POST /v1/users/login
      ✓ должен успешно авторизовать пользователя и вернуть JWT
      ✓ должен вернуть ошибку 401, если неверный пароль
      ✓ должен вернуть ошибку 401, если пользователь не найден
      ✓ должен вернуть ошибку 400, если отсутствует email
      ✓ должен вернуть валидный JWT токен с правильным payload

Test Suites: 1 passed, 1 total
Tests:       9 passed, 9 total
```

---

### 2️⃣ Orders Service - Jest тесты

```bash
cd project/orders-service

# Установка зависимостей (если еще не установлены)
npm install

# Запуск тестов
npm test

# Запуск конкретного набора тестов
npm test -- --testNamePattern="создание заказа"

# Запуск с подробным выводом
npm test -- --verbose
```

**Ожидаемый результат:**
```
PASS  app.test.js (3.124 s)
  Orders Service Tests
    POST /v1/orders - создание заказа
      ✓ должен успешно создать заказ
      ✓ должен вернуть ошибку 401, если нет токена
      ✓ должен вернуть ошибку 400, если items пусто
      ✓ должен вернуть ошибку 400, если totalAmount <= 0
    GET /v1/orders - получение заказов
      ✓ должен вернуть заказы текущего пользователя
      ✓ должен вернуть ошибку 401, если нет токена
      ✓ разные пользователи должны видеть только свои заказы
    ... (17 тестов всего)

Test Suites: 1 passed, 1 total
Tests:       17 passed, 17 total
```

---

## 🔧 Расширенные опции Jest

### Запуск всех тестов проекта

```bash
# Из корневой папки project/
npm run test-all

# Или запустить оба сервиса по очереди:
cd users-service && npm test && cd ../orders-service && npm test
```

### Генерация отчета о покрытии

```bash
cd project/users-service
npm test -- --coverage

# Результаты в папке coverage/
```

### Watch режим

```bash
cd project/orders-service
npm test -- --watch

# Запустится автоматически после каждого сохранения файла
```

### Отладка тестов

```bash
# С использованием Node Inspector
node --inspect-brk ./node_modules/jest/bin/jest.js --runInBand

# Или просто добавить debugger в тест:
// app.test.js
it('test name', () => {
  debugger; // Остановится здесь при запуске в режиме отладки
  // ...
});
```

---

## 📋 Структура тестов

### Users Service (9 тестов)

```
app.test.js
├── POST /v1/users/register
│   ├── ✓ должен успешно зарегистрировать нового пользователя
│   ├── ✓ должен вернуть ошибку 400, если email уже существует
│   ├── ✓ должен вернуть ошибку 400, если отсутствует email
│   └── ✓ должен вернуть ошибку 400, если отсутствует пароль
├── POST /v1/users/login
│   ├── ✓ должен успешно авторизовать пользователя и вернуть JWT
│   ├── ✓ должен вернуть ошибку 401, если неверный пароль
│   ├── ✓ должен вернуть ошибку 401, если пользователь не найден
│   ├── ✓ должен вернуть ошибку 400, если отсутствует email
│   └── ✓ должен вернуть валидный JWT токен с правильным payload
```

### Orders Service (17 тестов)

```
app.test.js
├── POST /v1/orders - создание заказа
│   ├── ✓ должен успешно создать заказ
│   ├── ✓ должен вернуть ошибку 401, если нет токена
│   ├── ✓ должен вернуть ошибку 400, если items пусто
│   └── ✓ должен вернуть ошибку 400, если totalAmount <= 0
├── GET /v1/orders - получение заказов
│   ├── ✓ должен вернуть заказы текущего пользователя
│   ├── ✓ должен вернуть ошибку 401, если нет токена
│   └── ✓ разные пользователи должны видеть только свои заказы
├── PATCH /v1/orders/:id - изменение заказа
│   ├── ✓ должен успешно изменить заказ текущего пользователя
│   ├── ✓ должен вернуть ошибку 403, если пользователь не является владельцем
│   ├── ✓ должен вернуть ошибку 404, если заказ не найден
│   └── ✓ должен вернуть ошибку 400, если заказ отменен
├── PATCH /v1/orders/:id/cancel - отмена заказа
│   ├── ✓ должен успешно отменить заказ
│   ├── ✓ должен вернуть ошибку 403, если заказ не принадлежит пользователю
│   ├── ✓ должен вернуть ошибку 404, если заказ не найден
│   └── ✓ должен вернуть ошибку 400, если заказ уже отменен
└── Защита от несанкционированного доступа
    ├── ✓ должен вернуть ошибку 401, если токен истек
    └── ✓ должен вернуть ошибку 403, если токен подделан
```

---

## 🚀 Postman тестирование

### Импорт Collection

1. Откройте Postman
2. `File` → `Import`
3. Выберите `Postman_Collection.json`
4. Нажмите `Import`

### Запуск последовательности тестов

1. Откройте `Microservices API Collection`
2. Нажмите `...` (три точки) → `Run collection`
3. Нажмите `Run Microservices API Collection`

### Переменные в Postman

Collection использует переменные:
- `{{jwt_token}}` - токен User 1
- `{{jwt_token_user2}}` - токен User 2
- `{{order_id}}` - ID созданного заказа

Переменные автоматически заполняются из ответов предыдущих запросов.

---

## 🔄 Curl команды для быстрого тестирования

### Запустить все тесты через curl

```bash
# Linux/Mac
bash project/test-api.sh

# Windows PowerShell
PowerShell -ExecutionPolicy Bypass -File project/test-api.ps1
```

### Отдельные curl команды

**1. Регистрация:**
```bash
curl -X POST http://localhost:3001/v1/users/register \
  -H "Content-Type: application/json" \
  -d '{"email":"user1@example.com","password":"password123"}'
```

**2. Логин:**
```bash
curl -X POST http://localhost:3001/v1/users/login \
  -H "Content-Type: application/json" \
  -d '{"email":"user1@example.com","password":"password123"}'
```

**3. Создание заказа:**
```bash
TOKEN="your_jwt_token_here"
curl -X POST http://localhost:3002/v1/orders \
  -H "Content-Type: application/json" \
  -H "Authorization: Bearer $TOKEN" \
  -d '{"items":[{"productId":1,"quantity":1,"price":100}],"totalAmount":100}'
```

---

## 📊 Интерпретация результатов

### Успешный тест

```
✓ должен успешно создать заказ (38ms)
```
- ✓ = Тест прошел
- (38ms) = Время выполнения

### Ошибочный тест

```
✗ должен успешно создать заказ
  Expected: 201
  Received: 400
```
- ✗ = Тест не прошел
- Показывается ожидаемое и полученное значение

### Пропущенный тест

```
○ должен успешно создать заказ
```
- ○ = Тест пропущен (закомментирован или `skip()`)

---

## 🐛 Troubleshooting

### Ошибка: "Port already in use"

```bash
# Windows
netstat -ano | findstr :3001
taskkill /PID <PID> /F

# Linux/Mac
lsof -i :3001
kill -9 <PID>
```

### Ошибка: "Cannot find module 'jest'"

```bash
npm install --save-dev jest supertest
```

### Тесты не запускаются

Проверьте:
1. Установлены ли все зависимости: `npm install`
2. Корректен ли путь: `npm test` запускается из папки с `package.json`
3. Jest конфиг существует: `jest.config.js`

### Тесты зависают

```bash
# Добавьте timeout
npm test -- --testTimeout=30000
```

---

## ✨ Best Practices

### Хорошие тесты

```javascript
✓ Одна концепция на тест
✓ Ясное описание: "должен..."
✓ Независимые тесты (не зависят друг от друга)
✓ Быстрые тесты (< 100ms)
✓ Проверяют поведение, а не реализацию
```

### Плохие тесты

```javascript
✗ Несколько проверок в одном тесте
✗ Нечеткие названия: "test1"
✗ Зависящие друг от друга тесты
✗ Медленные тесты (> 1s)
✗ Проверяют внутреннюю реализацию
```

---

## 📝 Примеры написания новых тестов

### Тест успешного сценария

```javascript
it('должен создать новый заказ', async () => {
  const token = createToken(1, 'user@example.com');
  
  const res = await request(app)
    .post('/v1/orders')
    .set('Authorization', `Bearer ${token}`)
    .send({
      items: [{ productId: 1, quantity: 1 }],
      totalAmount: 100
    });
  
  expect(res.statusCode).toBe(201);
  expect(res.body.order.status).toBe('pending');
});
```

### Тест ошибочного сценария

```javascript
it('должен вернуть 403 при попытке доступа к чужому заказу', async () => {
  const token = createToken(2, 'user2@example.com');
  
  const res = await request(app)
    .patch('/v1/orders/1')
    .set('Authorization', `Bearer ${token}`)
    .send({ totalAmount: 200 });
  
  expect(res.statusCode).toBe(403);
  expect(res.body.error).toContain('доступа');
});
```

---

## 🎯 Цели тестирования

- [x] Регистрация работает корректно
- [x] Авторизация генерирует валидный JWT
- [x] JWT проверяется на каждом защищенном маршруте
- [x] Пользователи видят только свои заказы
- [x] Пользователи не могут менять чужие заказы
- [x] Заказы нельзя менять после отмены
- [x] Все ошибки возвращают правильные HTTP коды

---

**Версия инструкции:** 1.0.0  
**Последнее обновление:** 6 декабря 2025
